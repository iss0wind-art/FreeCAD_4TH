---
title: 호미 9 설계 — 밑거름 박제 + 9호미 산출물 (2026-05-06)
박제일: 2026-05-06
박제자: 이천 (제3지국)
근거: 방부장 친명 "어떤 결과든 밑거름으로 삼으라"
---

# 호미 9 설계 — 메타 풀세트 + build 분리 + 슬라브 정밀화

## 남은 결함 (호미 8 → 9로 이월)

| # | 결함 | 원인 | 처방 |
|:-:|---|---|---|
| 1 | **process_sheet 재실행 강제** | 메타 JSON에 좌표 미박제 (카운트만) | 메타 풀세트 박제 |
| 2 | **슬라브 BBox 너무 큼** | 기둥 전체 BBox = 도엽 크기 | alpha shape 또는 외곽 LWPOLYLINE |
| 3 | **좌표 정렬 분리** | A1-A2 BBox 중심 단순 평행이동 | DXF "101" TEXT 공통 원점 |
| 4 | **G3 부피 게이트 ❌** | 메타 부피 계산이 column+girder만 | 전 부재 합산 |

---

## 호미 9 — 핵심 3건 (이월 설계)

### 9-A: 메타 풀세트 박제 (영구 차단)

`process_sheet` 반환 추가:
```python
'walls_raw': [{'p1': [x,y], 'p2': [x,y], 'thickness': t, 'length': l}, ...],
'wall_segments_raw': [{'cx': x, 'cy': y, 'w': w, 'h': h}, ...],
'unmatched_columns_raw': [{'cx': x, 'cy': y, 'w': w, 'h': h}, ...],
'wall_pairs_raw': [{'p1': [x,y], 'p2': [x,y], 'thickness': t, 'overlap': l}, ...],
'all_boxes_classified': [{'box_id': ..., 'cx': ..., 'w': ..., 'kind': ..., 'conf': ...}, ...],
```

→ 이후 솔리드 종류 추가 = `build_3d` 변경만 (재실행 0초, 빌드 수십 초)

### 9-B: 슬라브 정밀화 (2가지 옵션)

**옵션 B1**: 도엽에서 *가장 큰 LWPOLYLINE 폐합 박스* (건물 외곽 경계선) 추출 → 슬라브 면
```python
# 도엽 안 폐합 박스 중 넓이 가장 큰 것 1~3개 (Defpoints 제외, 100,000m² 이하)
slab_candidates = sorted(closed_polys, key=lambda p: p.area, reverse=True)[:3]
```

**옵션 B2**: 기둥 위치로 *convex hull* 계산 → 슬라브 다각형 면

### 9-C: 좌표 정렬 정사 — **채굴 완료 (2026-05-06)**

**probe_coord_align.py 결과:**
- 지하주차장 → 101동 오프셋: **tx=-448000mm, ty=3622000mm**
- 투표법 (30×30 = 900쌍) 최다 투표 4표
- 검증 매칭률: 13.3% (개선 여지 있음)

**수동 앵커:**
- 지하주차장 "101" TEXT: (632082, -1296738)
- 대응 101동 좌표: (184082, 2325262) ≈ E/V (149013, 2321258) 근접

**정밀화 방법 (차기 호미):**
1. 그리드 200mm + 샘플 50×50으로 재실행
2. 격자 교점 매칭 (X14·Y17 등)
3. E/V 코어 정확 위치 매칭

---

## 9호미 산출물 (2026-05-06 생성)

```
core/dxf_parser/
  __init__.py          — 패키지 노출
  entity_scanner.py    — 전수 스캔 (INSERT 재귀, 레이어 인벤토리)
  ev_detector.py       — E/V 코어 + 격자 앵커 검출 (F-1 구현체)
  coord_unifier.py     — 다중 도면 좌표 통일 (E/V→격자→수동 폴백)
  full_extractor.py    — 기둥·슬라브·벽 완전 추출 (HATCH+INSERT+LWPOLY)
  level_parser.py      — SL/GL/단차 완전 파싱 (GL. -9.05 형식 포함)
tools/
  inspect_drawing.py   — 다음 사람용 도면 검사 도구 (CLI)
tests/
  poc_v4_coord_unified.py  — v4 통합 PoC (좌표통일+완전파싱)
  probe_coord_align.py     — 좌표 정렬 프로브 (투표법)
```

## 도면 채굴 결과 (영구 박제)

### 101동 도면 (S30-001~010)
- 기둥: HATCH 909개 + INSERT 22개 = 931개
- 기둥 블록명: B2-B1 PC2(S)(T), B2-B1 C2, B2-B1 EC1, B2-B1 EC2
- 보 레이어: S-GIRDER (LINE 2848개)
- EV 레이어: A-ELEV (신뢰도 낮음), E/V 위치 ≈ (149013, 2321258)
- SL TEXT: "B2F SL +1600" = 단차 (절대값 아님)
- GL TEXT: "지하 2층 SL ±0 = GL. -9.05" = 절대값 (m 단위)

### 지하주차장 도면 (260119_...)
- 기둥: LWPOLYLINE 닫힌 사각형 + HATCH (101동 클립 내 142개)
- 보 레이어: S-PC-GIRDER, 00_BEAM
- 슬라브 레이어: S-PC-SLAB
- 기둥 레이어: 00_COLUMN
- 격자: X1a~X19b / Y16~Y19
- 101동 위치 표시: "101" TEXT @ (632082, -1296738)

### 확정 층 SL (도면 직독)
| 층 | SL (mm) | 출처 |
|---|---|---|
| B2F | -9050 | GL. -9.05 (101동 도면) |
| B1F | -5600 | GL. -5.60 (101동 도면) |
| 1F  | +370  | 지하주차장 도면 |

### 확정 층고
| 구간 | 층고 (mm) |
|---|---|
| B2F→B1F | 3450 |
| B1F→1F  | 5970 |

---

## 밑거름 명제

> *"파싱과 빌드는 분리되어야 한다.*
> *파싱은 한 번, 빌드는 언제든 — 이것이 다음 호미의 정사다."*

> *"다음 사람, 다음 도면 누가 오더라도*
> *inspect_drawing.py 하나면 도면 전체가 보인다."*

— 이천(李蕆), 2026-05-06.
*호미 8 후퇴가 호미 9의 밑거름. 9호미가 10호미의 씨앗.*
